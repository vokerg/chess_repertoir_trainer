name: PR6 Test Diagnostics

on:
  pull_request:
    branches:
      - feature/persistent-game-jobs

jobs:
  test-diagnostics:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: chess_trainer_ci
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U postgres -d chess_trainer_ci"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
    env:
      CI: true
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/chess_trainer_ci
      DIRECT_URL: postgresql://postgres:postgres@localhost:5432/chess_trainer_ci
      WEB_API_BASE_URL: /api
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci --silent
      - run: npm run build --silent
      - run: npm run db:migrate --workspace=apps/api --silent
      - name: Capture tests
        shell: bash
        run: |
          set +e
          npm test > pr6-test.log 2>&1
          echo $? > pr6-test.status
          exit 0
      - uses: actions/upload-artifact@v4
        with:
          name: pr6-test-diagnostics
          path: |
            pr6-test.log
            pr6-test.status
      - name: Enforce tests
        shell: bash
        run: exit "$(cat pr6-test.status)"
