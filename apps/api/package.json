{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "dev": "(cd ../.. && npm run build --workspace=packages/chess-domain) && ts-node-dev --respawn --transpile-only src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "db:migrate": "prisma migrate deploy",
    "db:seed": "ts-node prisma/seed.ts",
    "db:reset": "prisma migrate reset --force && npm run db:seed",
    "prisma:studio": "prisma studio",
    "test": "node -e \"console.log('No api tests yet')\""
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@prisma/client": "^5.6.0",
    "chess-domain": "file:../../packages/chess-domain",
    "chess.js": "^1.0.0",
    "dotenv": "^16.3.1",
    "fastify": "^5.8.5",
    "prisma": "^5.6.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.0",
    "vitest": "^4.1.7"
  },
  "prisma": {
    "schema": "prisma/schema.prisma"
  }
}
