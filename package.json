{
  "name": "chess-repertoire-trainer",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/api",
    "apps/mobile",
    "packages/chess-domain"
  ],
  "engines": {
    "node": ">=22.12.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "dev": "npm run build:domain && concurrently \"npm run dev --workspace=apps/api\" \"npm run dev:worker --workspace=apps/api\" \"npm run dev --workspace=apps/web\"",
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:worker": "npm run dev:worker --workspace=apps/api",
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:mobile": "npm run start --workspace=apps/mobile --",
    "ios:mobile": "npm run ios --workspace=apps/mobile",
    "build": "npm run build:domain && npm run build:api && npm run build:web",
    "build:domain": "npm run build --workspace=packages/chess-domain",
    "build:api": "npm run build --workspace=apps/api",
    "build:web": "npm run build --workspace=apps/web",
    "build:mobile": "npm run build --workspace=apps/mobile",
    "test": "npm run test --workspace=packages/chess-domain && npm run test --workspace=apps/api && npm run test --workspace=apps/web",
    "test:mobile": "npm run test --workspace=apps/mobile",
    "lint": "npm run lint --workspaces",
    "lint:mobile": "npm run lint --workspace=apps/mobile",
    "format": "npm run format --workspaces",
    "db:migrate": "npm run db:migrate --workspace=apps/api",
    "db:seed": "npm run db:seed --workspace=apps/api",
    "db:reset": "npm run db:reset --workspace=apps/api",
    "prisma:studio": "npm run prisma:studio --workspace=apps/api"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
