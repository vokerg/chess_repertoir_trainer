# RB-011 course reintegration closure

Date: 2026-07-29

Status: complete

Task: RB-011

GitHub issue: #99 — closed as completed

Implementation pull request: #189

Squash commit: `01b36f9503ccfbb3dced55d56589b89cfd163867`

Final review-package head: `30b077377cb5e9337dd455f9f8a8a7a38a152cec`

Final review-package CI: run `30472999505` / CI #1488 — success

## Closure decision

RB-011 is accepted and integrated on `main`.

The merged implementation provides the safe course-write boundary required by downstream builder adaptation:

- a pure completed-session projection into the existing analysis merge tree;
- explicit exclusion of pending, deferred, ignored and stale branches;
- authenticated mandatory preview and explicit apply operations;
- reviewed new-line or exact existing-line-anchor targets;
- created, reused, skipped and conflicting counts;
- a preview token bound to the authenticated user, full draft, destination, preview result and current course content revision;
- strict no-conflict behavior;
- transactional reuse of the existing move-node writer and content-revision path;
- equivalent-line and repeated-apply safety;
- feature-local Angular destination, preview and apply state;
- no Prisma model, migration, automatic write, whole-course orchestration or durable builder persistence.

## Validation accepted

The final review-package CI #1488 passed:

- lint;
- all builds;
- both opening-classification audits;
- architecture guardrails;
- database migrations;
- the complete repository test suite.

Focused coverage includes completed-draft projection, contract invariants, authentication and route validation, ownership, new-line creation, existing-line merge and node reuse, conflicts, stale preview and anchor rejection, transaction rollback, content revision behavior, repeated apply and Angular review/apply behavior.

The user explicitly approved squash-merging PR #189 on 2026-07-29. The repository and GitHub issue state therefore advance from `REVIEW` to `DONE`.

## Accepted product boundary

The first materialization slice writes only to an existing owned course and chapter. It does not create a course/chapter hierarchy or persist builder session/target metadata. Persisted course lines remain separate trees rather than shared graph nodes.

These are explicit v1 boundaries, not blockers for RB-012. Existing-course finding entry points can reuse the merged exact-preview/exact-anchor apply boundary and constrain it further where the source consequence is unambiguous.

## Downstream impact

RB-012 is unblocked.

Its first implementation slice should reuse RB-011 rather than add a second writer. Course-ending findings can bind the completed draft to one exact source line endpoint and use RB-011 preview as the authoritative ownership, conflict, staleness and course-revision check.

## Superseded review notes

The earlier implementation report remains the detailed architecture and validation record. Statements in that report that RB-011 is only “implemented for review,” that issue #99 is open, or that RB-012 is blocked are superseded by this closure report and the merged repository state.
